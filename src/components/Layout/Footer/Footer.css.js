import styled from "styled-components";

export const WrapperFooter = styled.div`
  grid-area: footer;
  padding-left: 1em;

  display: flex;
  justify-content: flex-end;
  align-items: center;

  button {
    margin-right: 15px;
  }
`;

export default {
  WrapperFooter,
};
