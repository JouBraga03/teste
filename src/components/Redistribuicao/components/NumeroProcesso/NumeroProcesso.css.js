import styled from "styled-components";
import Typography from "@material-ui/core/Typography";

export const TextNumeroProcesso = styled(Typography)({
  fontSize: "12px",
});

export default {
  TextNumeroProcesso,
};
